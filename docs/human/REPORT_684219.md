# Report conclusivo — attività 684219

**Esito: PASS CON WARNING.** L’evento udev `add` è stato riprodotto sul T7 reale
e ha avviato systemd end-to-end; non è stato eseguito un replug fisico né uno
scollegamento durante scrittura, per evitare un test distruttivo.

- Precedente: timer daily/weekly/monthly 583921, T7 normalmente montato, nessun
  trigger a collegamento, notifiche solo journal.
- Nuovo: udev minimale su disco USB+seriale → `SYSTEMD_WANTS` dello stesso
  `t7-restic-backup.service`; i tre timer incompatibili sono rimossi.
- Identità: Samsung PSSD T7 Shield, `S6YGNS0Y903440H`, ext4 direct, UUID
  `4c75ac03-4c73-43f8-afd9-f90db49a74fc`; LUKS **no**; mount
  `/mnt/T7_BACKUP`; repository `/mnt/T7_BACKUP/restic-fedora`.
- Unit create/modificate: backup lifecycle; reminder service+timer one-shot.
  Regola: `/etc/udev/rules.d/90-t7-name.rules`.
- Backup config precedente: `/var/lib/t7-restic-backup/install-backups/activity-684219-20260711T235110Z`.
- Trigger reale: PASS; due add simultanei → 1 start, 1 job.
- Snapshot trigger: `f7682be7`, 14,116s, 116.005.245.928 B letti,
  23.517.077 B fisici aggiunti. Snapshot finale test manutenzione: `3082eb92`,
  14,137s, 3.490.556 B fisici aggiunti.
- Retention: PASS, 7 daily/5 weekly/12 monthly/3 yearly; forget ogni backup.
- Check/prune: check 5% 198/198 pack PASS; prune PASS; full check non dovuto.
  Lo state `0600` registra epoch riusciti, non usa mtime.
- Sync/smontaggio: PASS; LUKS close non applicabile; finale T7 presente sul bus
  ma non montato.
- Telegram: config esistente validato; successo, errore e reminder inviati;
  marker per-job evita duplicati. Nessun token copiato in script/argv/Git.
- Collaudo notifiche 01:51–01:58 CEST: quattro errori `unmount-cleanup` furono
  emessi durante la diagnosi del namespace di mount prima della correzione;
  l'errore `backup` exit 97 fu una fault injection intenzionale. I successi
  successivi provano il recupero. Le future fault injection sono marcate
  esplicitamente `TEST CONTROLLATO` nelle notifiche.
- Reminder: test accelerato 5s con device presente inviato una volta; device
  assente simulato non invia; produzione ripristinata a 30m.
- Fallimento: injection pre-Restic exit 97, nessuna retention/prune, cleanup e
  notifica errore PASS; collegamento successivo `a989e8fd` PASS.
- Mount invalido: exit 21, entry interne `0→0`, nessuna scrittura/repository
  interno.

Comandi:

```bash
systemctl status t7-restic-backup.service t7-restic-reminder.timer
sudo journalctl -u t7-restic-backup.service -n 200
findmnt /mnt/T7_BACKUP
sudo systemctl start t7-restic-backup.service
```

File principali: lifecycle/core/metrics/notifier/reminder in `scripts/`, unità
in `systemd/`, regola in `udev/`, esclusioni in `config/`, documentazione e test.
Branch previsto `codex/684219-t7-connect-backup`; commit/push verificabili con
`git log -1` e `git ls-remote` dopo il gate finale.

Warning residui: password escrow del report 583921; singola copia locale non
off-site; il test usa udev replay sul device reale invece di replug fisico.
